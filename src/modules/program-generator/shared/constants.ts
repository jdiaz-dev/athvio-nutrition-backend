export const NEO4J_CONFIG: string = 'NEO4J_CONFIG';
export const NEO4J_DRIVER: string = 'NEO4J_DRIVER';



export enum ErrorDiseaseCausesEnum {
  EMPTY_NODE = 'Without data for nutritional disease causes',
  DISEASE_CAUSES_NOT_FOUND = 'Disease causes not found',
}

export enum ErroDiseasesEnum {
  EMPTY_NODE = 'Without data for diseases',
  DISEASES_NOT_FOUND = 'Diseases not found',
}

export enum ErrorNutritionalPreferencesEnum {
  EMPTY_NODE = 'Without data for nutritional preferences',
  NUTRITIONAL_PREFERENCES_NOT_FOUND = 'Nutritional preferences not found',
}
